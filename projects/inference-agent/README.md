# inference-agent

目标：增强现有 Agent 的研究判断。状态：planned。

Core 路径：定义输入模型/协议和输出 RunRecord→证据审计→Pareto→DeliveryReport。

验收：外部 Agent 以适配器接入；Agent EvalReport 与业务 DeliveryReport 分开，不复制生产仓库。每个阶段交付代码、正确性证据、实验合同和局限；Stretch 再扩展，不要求同时做四个项目。
