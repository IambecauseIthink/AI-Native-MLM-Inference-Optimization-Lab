# 02 教师指导 · GPU 带宽已满，还能提速吗？

## 带学节奏

- 0–5 分钟：讲具体情境“你最初判断“并发 1 已经拉满，增加并发可能无收益”。现在只讨论权重读取带宽：它满了还可能有收益吗？”。先给一个已解释的例子，不连续抛术语或测验。
- 5–10 分钟：共同画出对象/shape/时间线。基础只补学习页的一段，让学员用自己的话指认变量。
- 10–20 分钟：展示默认实验，再询问学习页的一个预测；允许未知。只改一个条件，显示前后结果。
- 20–28 分钟：解释公式与假设，邀请学员指出哪个成本被省略；不要把 toy 数字解释成硬件事实。
- 28–35 分钟：跟读以下真实 symbol，最多打开两个必要调用片段；较长调用链可留到下次。
- 35–45 分钟：做一个反例，记录学员原话、未知与下一步；未经学员明确完成判断，不更新为掌握。

## 结果与递进提示

默认是 HBM 上界更紧。batch=128 且 compute_tflops=10 时，计算下界超过搬运下界，换成 compute 上界更紧。修改 weight_bytes 只改变理想存储，不能假设硬件算力和 kernel 仍相同。

提示一：先看改变了哪个输入。提示二：指出公式中的对应项。提示三：一起算最小数字，再回到完整例子。不要用“显然”“你应该知道”评价学员。

## 真实实现导航

- [GPUModelRunner.execute_model](https://github.com/vllm-project/vllm/blob/2a69949bdadf0e8942b7a1619b229cb475beef20/vllm/v1/worker/gpu_model_runner.py#L3770) · `vllm/v1/worker/gpu_model_runner.py` · L3770。

在 execute_model 中找 scheduler_output、输入准备与 model forward。先理解输入如何成为一批，再探索 kernel；不要把这个入口存在视为该部署确实选择了这条 runner 分支。

不要把最新文档与固定源码混用；如果实际运行选择了别的 runner/backend，另记证据，不改写这份教学基准。

## 误区与边界

row/s 是一层矩阵的理论行吞吐，不是模型 TPS/QPS；prefill 多位置权重复用与 decode 多序列复用形状相似，但 attention/KV 成本不同。

反例：显存带宽饱和不代表每个输出已分摊最少字节；计算上限、KV 或 launch 又可能成为新瓶颈。

## 理解出口

学员能解释一个预测、指出一个假设、提出一条可以否证解释的观察。未达到时，把困难拆成更小例子，不重复整套讲义。

进度初始为 ready；根据真实对话填写 [progress](../progress.md)。本课远程实验仍是 designed，没有启动 GPU。
